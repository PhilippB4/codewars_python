def odd_or_even(arr):
    # Wir geben "even" zurück wenn die Zahl durch zwei teilbar ist und der Rest dabei = 0 ist 
    # Ansonsten geben wir "odd" zurück
    return "even" if sum(arr) % 2 == 0 else "odd"
