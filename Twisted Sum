def compute_sum(n):
    summe = 0
    
    # Jede Zahl in der gegebenen Range durchgehen
    for x in range(1, n+1):
        # Jede Ziffer in der Zahl zur Summe hinzuaddieren
        for d in str(x):
            summe += int(d)
    return summe
